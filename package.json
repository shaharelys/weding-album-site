{
  "name": "wedding-albums-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd wedding-albums && npm install && npm run build",
    "start": "cd wedding-albums && npm start"
  },
  "devDependencies": {
    "ffmpeg-static": "^5.2.0"
  }
}
